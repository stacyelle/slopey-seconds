
class searchBar() {
    constructor(searchResults) {
        console.log('Search is initializing');
        $('body').html('this is the search module');
        this.container = $(container);

        this.render();
            this.container.html("<div>This is the search</div>");
    };
};